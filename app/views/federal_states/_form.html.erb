<%= form_for @federal_state, html: { class: "pure-form pure-form-aligned" } do |f| %>
  <% if @federal_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@federal_state.errors.count, "error") %> prohibited this federal_state from being saved:</h2>

      <ul>
      <% @federal_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <%= f.label :country_id %><br>
    <%= f.text_field :country_id %>
  </fieldset>
  <fieldset>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </fieldset>
  <fieldset>
    <%= f.submit class: "pure-button pure-button-primary" %>
  </fieldset>
<% end %>
